import ActivateAccount from "@/karnx/pages/ActivateAccount";
import { Suspense } from "react";

const page = () => {
    return(
        <Suspense>
            <ActivateAccount/>
        </Suspense>
    )
}

export default page;