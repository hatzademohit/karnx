import ResetPassword from "@/karnx/pages/ResetPassword";
import { Suspense } from "react";

const page = () => {
    return(
        <Suspense>
            <ResetPassword/>
        </Suspense>
    )
}

export default page;
